embedding:
  backend: average_rgb
  model_name: hf-hub:laion/CLIP-ViT-B-16-laion2B-s34B-b88K
  batch_size: 8
  device: auto
  fallback: average_rgb
  save_embeddings_dir: D:/桌面/Deduplication_framework/outputs/test_run/embeddings
dedup:
  method: pairwise
  eps: 0.15
  max_candidates: 100
